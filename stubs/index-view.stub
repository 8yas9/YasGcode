<x-app-layout>
    <x-slot name="header">
        <div class="flex items-center justify-between">
            <h2 class="font-semibold text-xl text-gray-800">{{ModelName}} List</h2>
            <a href="{{ route('{{tableName}}.create') }}" class="px-4 py-2 bg-indigo-600 text-white rounded-lg text-sm hover:bg-indigo-500">Create</a>
        </div>
    </x-slot>

    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            <div class="bg-white overflow-hidden shadow-sm sm:rounded-lg p-6">
                @if(session('success'))
                    <div class="mb-4 px-4 py-2 bg-green-100 text-green-800 rounded-lg text-sm">{{ session('success') }}</div>
                @endif

                <table class="w-full text-sm">
                    <thead>
                        <tr class="border-b text-left">
                            <th class="py-2 px-1">#</th>
{{IndexFields}}
                            <th class="py-2 px-1">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach($items as $item)
                            <tr class="border-b hover:bg-gray-50">
                                <td class="py-2 px-1">{{ $item->id }}</td>
{{IndexFields}}
                                <td class="py-2 px-1 flex gap-2">
                                    <a href="{{ route('{{tableName}}.show', $item) }}" class="text-indigo-600 hover:underline">View</a>
                                    <a href="{{ route('{{tableName}}.edit', $item) }}" class="text-amber-600 hover:underline">Edit</a>
                                    <form action="{{ route('{{tableName}}.destroy', $item) }}" method="POST" onsubmit="return confirm('{{DeleteConfirmationMessage}}')">
                                        @csrf @method('DELETE')
                                        <button type="submit" class="text-red-600 hover:underline">Delete</button>
                                    </form>
                                </td>
                            </tr>
                        @endforeach
                    </tbody>
                </table>

                <div class="mt-4">
                    {{ $items->links() }}
                </div>
            </div>
        </div>
    </div>
</x-app-layout>
